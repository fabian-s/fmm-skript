/** Insight: the quotient approaches 2 although its value at h=0 is undefined. Colors: blue quotient, green limit band. Provenance: original. Formula g(h)=2+h verified in verify-konzepte-C4b/limit.mjs, 2026-08-19. */
import { useState } from "react"; import { Aufgabe,FMM_COLORS,Plot,Slider,Verdikt,fmtDe } from "../../lib";
export function LimitWidget(){const[h,setH]=useState(1.2);const g=(t:number)=>Math.abs(t)<1e-9?NaN:2+t;const near=Math.abs(h)<.1;return <div className="mt-2 rounded bg-slate-700/60 p-2"><Aufgabe>Schieben wir h von beiden Seiten gegen null und beobachten wir den Differenzenquotienten.</Aufgabe><Plot series={[{f:g,color:FMM_COLORS.blau,label:"g(h)"}]} xDomain={[-1.6,1.6]} yDomain={[0,4]} xLabel="h" yLabel="g(h)" readout markers={[{x:h,y:g(h),color:FMM_COLORS.blau,label:"aktuell"},{x:0,y:2,color:FMM_COLORS.gruen,label:"offener Punkt"}]} hlines={[{at:2,color:FMM_COLORS.gruen,dash:[4,3],label:"Grenzwert"}]}/><Slider label="h" value={h} onChange={setH} min={-1.5} max={1.5} step={.01} accent={FMM_COLORS.blau}/><Verdikt kind={near?"ok":"neutral"}>{near?"Nahe bei null liegt g(h) nahe bei 2, obwohl bei h = 0 ein offener Punkt bleibt.":<>g({fmtDe(h,2)}) = {fmtDe(g(h),2)}; verkleinern wir |h| weiter.</>}</Verdikt></div>}
