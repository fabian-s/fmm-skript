export const title = "Dreiecksmatrix";

import { TriangularMatrixWidget } from "./widgets/TriangularMatrixWidget";

Eine quadratische :k[Matrix]{#matrix} heißt
*Dreiecksmatrix* (engl. *triangular matrix*), wenn alles
auf einer Seite ihrer Hauptdiagonale null ist. Stehen die Nullen
unterhalb der Diagonale, ist die Matrix eine *obere*
Dreiecksmatrix; stehen sie oberhalb, eine *untere*:

$$
\bU = \begin{bmatrix} 2 & 1 & 3 \\ 0 & 4 & 5 \\ 0 & 0 & 6 \end{bmatrix}, \qquad \bL = \begin{bmatrix} 2 & 0 & 0 \\ 7 & 4 & 0 \\ 1 & 5 & 6 \end{bmatrix}.
$$

Warum die numerische lineare Algebra von dieser Form besessen ist: Ein
lineares Gleichungssystem mit Dreiecksmatrix ist fast schon gelöst. In
$\bU\bx = \bb$ lautet die letzte Gleichung
$6\,x_3 = b_3$ (eine Gleichung, eine Unbekannte), und jede
Gleichung darüber bringt nur eine neue Unbekannte hinzu. Das ganze
System entwirrt sich also durch
:k[Einsetzen]{#triangular-solve}, ganz ohne
Tricks; bei einer Null auf der Diagonale ist die Matrix singulär und das
Einsetzen bricht ab. Genau deshalb stecken Algorithmen wie das
:k[Gaußsche Eliminationsverfahren]{#gaussian-elimination}
und die :k[LU-Zerlegung]{#lu-decomposition}
ihre ganze Arbeit darin, Dreiecksform zu *erreichen*, und
deshalb konstruiert das QR-Verfahren (vgl. Heath §3.5) eine
:k[orthogonale Matrix]{#orthogonal-matrix}
$\bQ$ gerade so, dass
$\bQ^T \bA$ obere Dreiecksform bekommt.

Welche Seite der Diagonale muss für die jeweilige Dreiecksform verschwinden?

<TriangularMatrixWidget />

Das Nullmuster zeigt zugleich, warum oberes und unteres Dreieckssystem in entgegengesetzter Richtung eingesetzt werden.
