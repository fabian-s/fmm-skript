export const title = "Folge";

import { SequenceDots } from "./widgets/SequenceWidget";

{/* Konzept-Tooltip: Folge: eine geordnete unendliche Liste von Zahlen. */}

Eine Folge (sequence) ist eine unendliche, *geordnete* Liste von
Zahlen

$$
a_1,\; a_2,\; a_3,\; a_4,\; \dots,
$$

wobei der Index die Position angibt: $a_1$ ist das erste
Glied, $a_{17}$ das siebzehnte. Formal ist eine Folge einfach eine
:k[Funktion]{#function}, die jedem Index $n$ eine Zahl $a_n$
zuordnet; wir zählen ab $n = 1$. Die Reihenfolge zählt: Die Liste
$1, \tfrac{1}{2}, \tfrac{1}{3}, \dots$ (also
$a_n = 1/n$) ist eine andere Folge als dieselben Zahlen in
durchgewürfelter Reihenfolge.

Folgen sind das Grundwerkzeug hinter der
:k[Konvergenz]{#convergence}: Viele Folgen pendeln sich mit wachsendem $n$ auf
einen festen Wert ein. Für $a_n = 1/n$ ist er $0$: Zu jedem
$\varepsilon > 0$ ist $1/n < \varepsilon$, sobald $n > 1/\varepsilon$. Und
summieren wir die Glieder einer Folge nacheinander auf, entsteht eine
:k[unendliche Reihe]{#infinite-series}.

Wie bleibt die Reihenfolge der Glieder im Punktbild einer Folge erkennbar?

<SequenceDots />

Die Punkte verbinden Index und Wert, auch wenn sie sich dem Grenzwert
annähern – sichtbar sind allerdings nur die ersten zwanzig.
