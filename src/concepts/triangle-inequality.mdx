export const title = "Dreiecksungleichung";

import { TriangleWidget } from "./widgets/TriangleInequalityWidget";

{/* Konzept-Tooltip: Dreiecksungleichung (normale und umgekehrte Form). */}

Anschaulich: der direkte Weg ist nie länger als der Umweg. Laufen wir
vom Ursprung erst entlang des
:k[Vektors]{#vector} $\ba$ und von
dessen Spitze aus weiter entlang $\bb$, landen wir bei
$\ba + \bb$. Die drei Wege bilden ein Dreieck, und die
„Luftlinie" ist höchstens so lang wie der Umweg über die Ecke:

$$
\lVert \ba + \bb \rVert \;\leq\; \lVert \ba \rVert + \lVert \bb \rVert.
$$

Diese *Dreiecksungleichung* (triangle inequality) ist eine der
drei definierenden Eigenschaften jeder
:k[Norm]{#norm}. Sie gilt also nicht nur für
die :k[euklidische Norm]{#euclidean-norm},
sondern etwa auch für Summen- und Maximumsnorm. Für reelle Zahlen ist
sie die aus der Analysis vertraute Betragsungleichung
$|a + b| \leq |a| + |b|$. Kurzer Check mit
$\ba = (3, 0)^\top$ und $\bb = (0, 4)^\top$:
euklidisch ist
$\lVert \ba + \bb \rVert = 5 \leq 3 + 4 = 7$. Gleichheit
gilt in der euklidischen Norm genau dann, wenn einer der Vektoren ein
nichtnegatives Vielfaches des anderen ist.

**Umgekehrte Dreiecksungleichung.** Oft brauchen wir eine
Schranke „von unten": Wie verschieden können die Normen zweier Vektoren
höchstens sein? Wenden wir die Dreiecksungleichung auf den Umweg
$\ba = (\ba - \bb) + \bb$ an, folgt
$\lVert \ba \rVert \leq \lVert \ba - \bb \rVert + \lVert \bb \rVert$,
also
$\lVert \ba \rVert - \lVert \bb \rVert \leq \lVert \ba - \bb \rVert$.
Vertauschen wir die Rollen von $\ba$ und $\bb$,
gilt dieselbe Schranke auch für
$\lVert \bb \rVert - \lVert \ba \rVert$. Zusammen:

$$
\bigl|\, \lVert \ba \rVert - \lVert \bb \rVert \,\bigr| \;\leq\; \lVert \ba - \bb \rVert.
$$

Diese *umgekehrte Dreiecksungleichung* (reverse triangle
inequality) sagt: liegen zwei Vektoren nahe beieinander, dann sind auch
ihre Normen nahe beieinander, die Norm ist also eine stetige Funktion.
Genau so wird sie in Fehlerabschätzungen benutzt: der Abstand
$\lVert \ba - \bb \rVert$ zweier Größen kontrolliert die
Differenz ihrer Längen. Im Beispiel oben:
$|3 - 4| = 1 \leq \lVert \ba - \bb \rVert = 5$.

<TriangleWidget />
