export const title = "Niveaumengen (Höhenlinien)";

import { LevelSetWidget } from "./widgets/LevelSetsWidget";

{/*
 * Concept tooltip: Niveaumengen (Höhenlinien) einer Funktion mehrerer
 * Variablen.
 */}

Denken wir an eine topografische Wanderkarte: Kurven verbinden alle
Punkte gleicher Höhe. Für eine Funktion
$f\colon \R^2 \to \R$ gilt dasselbe Bild: die
*Niveaumenge* (level set) zur Höhe $c$ sammelt alle
Eingaben, an denen $f$ genau diesen Wert annimmt:

$$
\{\, \bx \in \R^n : f(\bx) = c \,\}.
$$

In zwei Dimensionen sind das typischerweise Kurven (Höhenlinien, contour
lines), im $\R^n$ typischerweise eine $(n-1)$-dimensionale Hyperfläche; an
kritischen Werten kann sie entarten. Zeichnen wir verschachtelte
Höhenlinien für mehrere Werte von $c$, wird die Gestalt einer
Funktion auf einen Blick sichtbar: Eng beieinander liegende Linien
bedeuten steiles Gelände, weit auseinander liegende flaches, und
kleine geschlossene Schleifen umschließen ein Minimum oder Maximum.

Für die Optimierung sind zwei Fakten wichtig. Erstens: Wo
$\nabla f(\bx) \neq \bnull$ ist, steht der
:k[Gradient]{#gradient}
$\nabla f(\bx)$ senkrecht auf der Niveaumenge durch
$\bx$ und zeigt in Richtung größerer Werte von $f$;
$-\nabla f$ ist also lokal der steilste Weg bergab. Zweitens:
Nahe einem Minimum sehen die Höhenlinien einer glatten Funktion aus wie
verschachtelte Ellipsen, deren Form die Hesse-Matrix bestimmt. Stark
gestreckte Ellipsen signalisieren ein schlecht konditioniertes Problem.

Bleibt der Gradient auch dann senkrecht zur Höhenlinie, wenn wir den Punkt entlang der Linie verschieben?

<LevelSetWidget />

Die orthogonale Beziehung gilt lokal an jedem regulären Punkt der Höhenlinie.
